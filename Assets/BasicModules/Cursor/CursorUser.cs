using Game;
using UnityEngine;

namespace BasicModules
{
    public class CursorUser : MonoBehaviour
    {
        private void OnEnable()
        {
            //GlobalContext.GetGameCursor().AddCursorUser(gameObject);
        }
    }
}